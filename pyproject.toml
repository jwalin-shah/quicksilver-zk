[build-system]
requires = []
build-backend = "_quicksilver_build_backend"
backend-path = ["."]

[project]
name = "tensor-quicksilver-zk"
version = "0.1.0"
description = "Pedagogical pure-Python QuickSilver zero-knowledge proofs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
